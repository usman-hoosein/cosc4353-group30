const Pricing = require("../models/pricing");

exports.getFuelHistory = (req, res, next) => {
  //TODO: Get Fuel Quote History data
};

exports.postFuelQuote = (req, res, next) => {
  //TODO: Add Fuel Quote form to db, when db is working
  res.send({ message: "OK" });
};
